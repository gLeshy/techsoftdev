#include<iostream>
#include<math.h>

using namespace std;
int main()
{int j=0;
    for(int f=2;;f++)
if(j==10001)
{
 }
    }

}
bool prime(int n)
{
    for(int i=2;i<=sqrt(n);i++)
        if(n%i==0)
            return 0;
    return 1;
}
